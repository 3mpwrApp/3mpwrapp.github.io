feat(admin): paginated JSON export for Admin Audit

- Add listAdminAuditAll with batching to export >1000 events
- Wire "Export JSON (All)" button in Admin Panel using expo-file-system + sharing

perf(resources): code-split Evidence Locker

- Route now lazy-loads implementation via React.lazy
- Impl kept minimal for now to keep tests green; full UI can be moved into impl incrementally

chore: lint tidy

- Fix import/order lint warning in Evidence Locker impl

feat(app): code-split Admin and Settings with Jest-aware lazy wrappers; stabilize Evidence Locker tests

- Admin and Settings: moved full implementations into `index.impl.tsx` and `settings.impl.tsx`.
	Route files now act as Jest-aware lazy wrappers (React.lazy at runtime, sync `require` under tests).
- Evidence Locker: kept the minimal implementation with Export/Import and passphrase modal; route stays as a Jest-aware lazy wrapper.
- Test env: broadened detection to include `NODE_ENV==='test'` in wrappers to avoid Suspense fallbacks in Jest.
- Admin Audit: JSON export (paged) remains wired with pagination helpers; verified via tests.
- Validation: Lint clean; all tests pass locally after changes.

Refs: performance/code-splitting initiative; >1000 events JSON export requirement.
