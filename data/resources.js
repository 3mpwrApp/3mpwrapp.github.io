// JavaScript mirror of resources.ts for Node-based scripts.
// Keep in sync with resources.ts. Consider generating programmatically if divergence risk grows.

const resources = require('./resources.json');

module.exports = { resources };
