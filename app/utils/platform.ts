export * from '../../utils/platform';
// Provide a dummy default export so Expo Router (which scans app/) does not warn.
// This module just re-exports helpers and is not a screen.
export default {};
